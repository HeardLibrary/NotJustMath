import "./ReviewSubheader.css";
const ReviewSubheader = () => {
    return (
        <div class="subheader">
        <span class="option active">Review</span>
        <span class="option">Approved</span>
        <span class="option">Rejected</span>
        </div>

    );
};

export default ReviewSubheader;